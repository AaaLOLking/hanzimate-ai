# AI 中文学习 Agent — Web MVP 项目计划书

> 版本：v0.1  
> 日期：2026-08-14  
> 项目阶段：首次 Agent 开发 / Web MVP  
> 建议周期：10 周（1 名全职开发者；若兼职，按 16–20 周估算）

## 1. 执行摘要

本项目拟开发一款面向来华留学生的 AI 中文学习 Agent，覆盖 HSK 备考、日常对话训练、系统课程、个性化错误记忆与间隔复习。产品的核心不是普通聊天机器人，而是一位能长期理解学习者、持续调整难度、记录证据并主动安排复习的中文教练。

MVP 建议聚焦：

- 18 岁以上、当前约 HSK 2–4 水平的来华留学生；
- 英文为首个辅助界面语言，中文为学习目标语言；
- “校园与城市生活口语 + HSK 2–4 核心能力”两条学习主线；
- 网页端优先，桌面与移动端仅保证响应式可用；
- 中国内地云服务部署，先进行 20–50 人封闭测试。

首版最重要的闭环是：

1. 用户完成目标与水平诊断；
2. 进行一段可打断、低延迟的实时中文对话；
3. 系统生成可核对的错误与亮点；
4. 错误进入个人学习档案；
5. 系统按记忆规律生成复习任务；
6. 下一次对话或课程能真正使用这些历史信息。

### 1.1 建议立即锁定的技术决策

| 领域 | MVP 建议 | 理由 |
|---|---|---|
| 文本主模型 | DeepSeek-V4-Flash，默认非思考模式 | 官方已正式提供该型号；便宜、快速、支持 JSON 与工具调用，适合问答、总结和结构化错误提取 |
| 高质量内容生成 | DeepSeek-V4-Pro，仅用于后台课件生成与抽检 | 把高成本能力留在非实时、可缓存的作者工作流 |
| 实时语音 | Qwen3.5-Omni-Flash-Realtime + WebRTC | 浏览器低延迟链路、语音输入输出、VAD、语义打断、转写与工具调用可在同一实时会话中完成 |
| 发音专项评测 | 科大讯飞流式语音评测，按练习调用 | 实时对话模型不等于专业发音评分；专项接口可返回声韵、声调、完整度、流畅度等维度 |
| 主数据库 | PostgreSQL + pgvector | 一套数据库同时容纳用户、课程、事件、JSONB 与向量；MVP 无需单独部署 Milvus、Pinecone 或图数据库 |
| 向量模型 | 百炼 text-embedding-v4，1024 维 | 多语言、成本低、接入简单；后续可替换为自托管 BGE-M3 |
| 复习算法 | FSRS + 用户可选复习节奏 | 比固定“1/3/7 天”更个性化，同时保留用户对每周频率和提醒时间的控制 |
| 课程知识 | 版本化 Markdown/MDX Wiki | 采用 Karpathy LLM Wiki 的“原始资料 → 持续编译的 Wiki”思想，适合可审计、可引用、可复用的教学知识 |
| 用户长期记忆 | 结构化画像 + 事件账本 + pgvector | 用户水平、错误次数、到期时间属于确定数据，不能仅依赖模糊向量召回 |

### 1.2 最关键的架构结论

“向量数据库还是 Wiki”并不是正确的二选一。

- **课程与教学知识**适合 Wiki：内容需要版本、引用、人工审核、交叉链接和长期积累。
- **用户画像与学习状态**适合关系数据库：等级、能力分、错误次数、掌握度、复习时间需要可计算、可审计。
- **历史对话与相似案例检索**适合向量索引：用来找语义相关的过往错误、例句和学习证据。

因此 MVP 应采用三层记忆，而不是把所有信息都塞进向量库或 Markdown。

---

## 2. 产品定位

### 2.1 一句话定位

一位面向来华留学生、同时理解 HSK 考试与中国日常生活场景、会长期记住学习证据并主动安排复习的 AI 中文教练。

### 2.2 用户核心问题

- 在真实中文环境中不敢开口，普通课程缺少高频陪练；
- HSK 学习与真实交流割裂，会做题但不会说；
- 聊天 AI 每次都像第一次见面，不记得长期错误；
- 纠错太多会破坏交流，纠错太少又难以进步；
- 教材和题目很多，但缺少适合个人水平的学习路径；
- 学过的错误没有被系统地再次测试，短期“会了”不等于长期掌握。

### 2.3 产品差异化

1. **对话、课程、错题和复习共享同一学习者模型**，不是四个孤立功能。
2. **每条长期记忆带证据、置信度和时间**，用户可查看、纠正、删除。
3. **HSK 与生活任务双轨设计**，例如“完成食堂点餐”同时训练数量表达、量词和礼貌策略。
4. **教学 Skill 可打包、版本化和评测**，逐步成为项目的核心知识资产。
5. **模型可替换**，产品价值不绑定某一家模型厂商。

### 2.4 MVP 不做什么

- 不覆盖所有 HSK 1–9 级；
- 不做官方 HSK 成绩预测或证书承诺；
- 不把开放式闲聊中的 ASR 结果当作严格发音分数；
- 不做教师、班级、学校 SaaS 后台；
- 不做原生 iOS/Android App；
- 不训练基础大模型，不做模型微调；
- 不自动抓取、复制并商业化使用未获授权的教材；
- 不长期默认保存原始录音。

---

## 3. 用户画像与首版边界

### 3.1 核心用户

**画像 A：初到中国的生活适应型学习者**

- 当前水平：HSK 2 左右；
- 目标：点餐、购物、问路、打车、宿舍和校园沟通；
- 痛点：词汇知道但听不懂自然语速，不敢开口；
- 高频功能：场景对话、慢速重说、关键词复习。

**画像 B：在校 HSK 备考型学习者**

- 当前水平：HSK 3–4；
- 目标：通过考试、满足入学或毕业要求；
- 痛点：语法、听力与写作错误反复出现；
- 高频功能：系统课程、真题结构拆解、错题复习、模拟练习。

**画像 C：高频交流提升型学习者**

- 当前水平：HSK 4 左右；
- 目标：课堂讨论、实习、社交表达更自然；
- 痛点：表达能被理解，但搭配、生硬表达和语用不自然；
- 高频功能：自由对话、表达改写、语域和文化说明。

### 3.2 首次建档字段

- 母语、可使用的辅助语言；
- 当前 HSK 级别、是否参加过考试、最近分项成绩；
- 学习目标及期限；
- 每周可学习时间；
- 优先场景：生活、校园、考试、实习、社交；
- 纠错偏好：即时、每轮结束、整场结束；
- AI 语速、是否显示拼音、是否先给英文解释；
- 用户自评与 10–15 分钟诊断结果；
- 复习频率、提醒时间与免打扰时段；
- 录音保存选择和隐私授权。

---

## 4. MVP 功能范围

### 4.1 P0：必须完成

#### A. 登录、建档与水平诊断

- 邮箱登录；
- 完成学习任务 Mission；
- 词汇、语法、阅读、听辨、口语自评组合诊断；
- 生成初始能力雷达图；
- 允许用户修正系统判断；
- 首次生成 7 天学习计划。

#### B. Codex 风格工作区

- 左侧为产品板块与工作区历史；
- 中间为当前课程、对话或复习任务；
- 右侧为上下文聊天与“本次学习目标”；
- 每节课和每次对话都是一个可恢复的 workspace；
- 左侧列表显示状态：未开始、进行中、待复习、已完成。

#### C. 实时对话训练

- WebRTC 麦克风连接；
- 服务端 VAD 与语义打断；
- AI 实时语音与同步转写；
- 用户可切换慢速、正常语速；
- 三种纠错策略：沉浸、教练、考试；
- 场景模板：食堂、便利店、问路、宿舍、医院挂号、课堂提问；
- 对话结束后生成摘要、关键词、亮点、错误和下一步建议；
- 用户可逐条确认或驳回错误；
- 保存文本记录；原始音频默认不长期保存。

#### D. 课程系统

- 首版内置 12–18 节短课；
- 每课只解决一个明确能力目标，建议 8–15 分钟完成；
- 内容包含：目标、必要知识、例子、即时练习、真实任务、检索练习；
- 右侧聊天框只注入当前课的压缩上下文；
- 每课结束产生学习证据，而不是仅记录“已浏览”；
- 支持继续上次进度。

#### E. 错误簿与长期画像

- 展示错误类型、原句、修改建议、解释、证据、出现次数；
- 聚类重复错误；
- 区分“偶然失误、可能薄弱、稳定薄弱、已掌握”；
- 用户可编辑、删除或标记“不再提醒”；
- 所有能力结论可追溯到一次对话、课程练习或测验。

#### F. 定期复习

- 用户选择每天、每周若干天或仅周末；
- FSRS 计算单个知识项的到期时间；
- 复习形式不只做卡片，包括回忆、纠错、听辨、复述、最小对立、角色扮演；
- 复习后记录 Again / Hard / Good / Easy；
- 过期任务进入今日队列，但不无限堆积。

#### G. 模型配置后台

- 管理员维护 provider、model、能力、价格快照、限流和启停状态；
- 文本模型允许高级用户切换；
- 实时语音模型首版固定默认值，避免不同协议造成 UI 分裂；
- API 密钥使用环境变量或密钥服务，不在数据库明文保存；
- 记录每次调用的模型、延迟、Token、费用估算和错误码；
- 支持健康检查与 fallback。

### 4.2 P1：MVP 稳定后

- HSK 模拟套卷与分项计时；
- 图片/摄像头识字和生活场景问答；
- 用户上传合法教材后的个人课程生成；
- 教师人工审核与课程发布台；
- 多辅助语言 UI；
- 周报、家长或教师分享；
- 支付与订阅；
- 更多国产模型路由；
- 用户自定义 AI 音色。

---

## 5. 核心交互设计

### 5.1 桌面布局

~~~text
┌─────────────────┬────────────────────────────────────┬──────────────────────┐
│ 学习空间         │ 当前工作区                          │ 本课 AI 教练          │
│                  │                                    │                      │
│ 今日             │ 课程内容 / 实时对话 / 错误回顾     │ 当前目标              │
│ 对话训练         │                                    │ 允许提问              │
│  ├ 食堂点餐      │ 主任务、转写、练习和反馈            │ 引用本课知识回答       │
│  └ 医院挂号      │                                    │ 不擅自切换课程         │
│ 系统课程         │                                    │                      │
│  ├ 第 01 课      │                                    │                      │
│  └ 第 02 课      │                                    │                      │
│ 错误回顾         │                                    │                      │
│ 学习档案         │                                    │                      │
└─────────────────┴────────────────────────────────────┴──────────────────────┘
~~~

移动端使用底部导航，右侧聊天改为抽屉；实时语音界面以大号状态、转写和打断按钮为主。

### 5.2 实时对话状态机

1. 选择场景和难度；
2. 系统生成本次 Context Pack；
3. 浏览器生成 Offer SDP，后端代持 API Key 并代理鉴权交换，浏览器建立 WebRTC；
4. 实时模型处理音频、VAD、打断与语音输出；
5. 前端持续显示用户与 AI 的转写；
6. 每完成一轮，仅把必要事件写入会话日志；
7. 每 3–5 分钟生成滚动摘要并裁剪旧音频上下文，控制成本与漂移；
8. 结束会话后异步执行总结、错误提取和画像更新；
9. 用户确认结果后，错误进入长期学习档案和复习队列。

### 5.3 纠错策略

| 模式 | 行为 | 适合人群 |
|---|---|---|
| 沉浸模式 | 不打断表达；结束后集中反馈 3 个高价值问题 | 怕开口、需要流利度训练 |
| 教练模式 | 每轮最多指出 1 个阻碍理解或高频问题 | 默认模式 |
| 考试模式 | 不提示；结束后按任务标准评分 | HSK 模拟与阶段测验 |

系统必须避免“一句话纠正五处”的挫败式体验。错误优先级建议为：阻碍理解 > 本课目标 > 高频复现 > 语用不自然 > 可有可无的润色。

---

## 6. Teach Skill 的产品化改造

本机 Teach Skill 的教学哲学可直接转化为产品核心，但文件系统结构应改造成多用户、可版本化的数据结构。

| Teach Skill 概念 | 产品中的实现 |
|---|---|
| MISSION.md | 每个用户的 Learning Mission：目标、期限、成功标准、约束、暂不学习内容 |
| RESOURCES.md | 课程来源库：官方标准、授权教材、教师资料、官方样题及来源说明 |
| lessons/*.html | 版本化 Lesson/MDX，每节课短小、自包含、只有一个关键胜利 |
| reference/*.html | 词汇表、语法速查、发音图示、考试策略等可复用参考页 |
| learning-records/*.md | 学习证据记录：用户已经证明会什么、纠正了什么误解、解锁什么后续内容 |
| NOTES.md | 用户教学偏好、语速、解释语言、纠错风格和场景偏好 |
| 最近发展区 | 根据已掌握证据和待掌握目标动态选择下一课，难度“刚好够得着” |
| 流利度与存储强度 | 对话训练即时表达；检索练习和间隔复习负责长期保持 |

### 6.1 课程内容流水线

1. **资料入库**：保存不可变原始资料、授权信息、版本和哈希；
2. **知识编译**：提取概念、词汇、语法、题型、易错点和出处，写入课程 Wiki；
3. **课程设计**：按 HSK 等级、生活任务、先修关系和教学 Skill 生成课件草稿；
4. **自动评审**：检查目标是否单一、例句是否自然、难度是否越级、答案是否唯一、引用是否存在；
5. **人工抽检**：MVP 阶段所有发布课程都需人工审核；
6. **版本发布**：课程版本冻结，旧学习记录继续指向原版本；
7. **反馈回流**：把高频误解、低通过率题目提交为课程修订候选，而非直接自动改教材。

### 6.2 每课 Context Pack

开始课程时只注入：

- 当前学习 Mission 的相关部分；
- 当前水平和辅助语言；
- 本课目标、必要知识、例句和答案规则；
- 与本课相关的最多 5 条活跃错误；
- 用户已证明掌握的先修知识；
- 教学与安全策略；
- 来源引用。

不注入完整历史聊天、完整教材或全部用户画像。这样可以降低成本、幻觉和隐私暴露。

---

## 7. 核心知识产权：Chinese Learning Coach Skill

建议从第一周就把教学能力从业务代码中分离，建立独立版本包：

~~~text
skills/chinese-learning-coach/
├─ SKILL.md
├─ references/
│  ├─ pedagogy.md
│  ├─ hsk-framework.md
│  ├─ error-taxonomy.md
│  ├─ pronunciation.md
│  ├─ lesson-contract.md
│  ├─ review-policy.md
│  └─ safety-and-culture.md
├─ schemas/
│  ├─ error-event.schema.json
│  ├─ learner-profile.schema.json
│  ├─ lesson.schema.json
│  └─ session-summary.schema.json
├─ prompts/
│  ├─ tutor-system.md
│  ├─ error-extractor.md
│  ├─ lesson-author.md
│  └─ lesson-reviewer.md
├─ rubrics/
│  ├─ conversation-feedback.md
│  ├─ lesson-quality.md
│  └─ hsk-alignment.md
└─ evals/
   ├─ gold-errors.jsonl
   ├─ tutor-cases.jsonl
   └─ lesson-cases.jsonl
~~~

真正的知识产权不应只有一份系统提示词，而应包括：

- 可执行的错误分类体系；
- 学习者状态更新规则；
- 课程生成合同和教学 Rubric；
- 经过专家校验的高质量例子；
- 模型评测集及回归结果；
- HSK 能力与生活任务的映射图；
- 长期积累的匿名化学习效果数据。

每次 Skill 更新必须有版本号、变更记录和自动评测；线上会话记录具体使用的 Skill 版本，便于回溯。

---

## 8. 长期记忆与用户画像方案

### 8.1 三层记忆

#### 第一层：确定性学习状态（PostgreSQL）

保存可计算事实：

- 当前目标、期限、辅助语言；
- 听说读写译能力估计；
- 已掌握词汇与语法；
- 错误次数、最近出现、复习到期；
- 课程进度、测试结果；
- 隐私授权和保留策略。

这些数据不能只存在向量库，因为系统需要准确回答“今天到期多少条”“这个错误出现过几次”“为什么把我判断为 HSK 3”。

#### 第二层：证据与事件账本（PostgreSQL + 对象存储）

所有状态变化来自不可变事件：

- 对话中的一句原话；
- 一道题的作答；
- 一次发音评测；
- 用户对 AI 纠错的确认或驳回；
- 一次复习评分。

聚合画像可以重算，证据不可悄悄覆盖。原始音频只在明确同意时进入对象存储，并设置自动删除。

#### 第三层：语义召回（pgvector）

为会话摘要、错误证据、课程段落和参考页生成向量，用于：

- 查找相似历史错误；
- 检索相关例句和课程；
- 在对话前选择最相关的少量记忆；
- 检查课程内容是否重复。

向量检索结果只是候选证据，不能直接改写等级或掌握状态。

### 8.2 Karpathy Wiki 放在哪里

Karpathy 的 LLM Wiki 强调：保存原始资料，让 LLM 把资料持续“编译”为互相链接、可积累的 Wiki，而不是每次从原始分块重新拼答案。这个思想很适合：

- HSK 标准和题型知识；
- 语法、词汇、发音和语用知识；
- 生活场景剧本；
- 教师审定的课程策略；
- 项目自己的教学经验。

它不适合直接作为主要的个人进度数据库，因为 Markdown 页面不擅长并发更新、事务、精确调度和隐私权限。建议课程 Wiki 存于 Git/对象存储，发布后同步到数据库检索表。

### 8.3 错误事件建议结构

| 字段 | 说明 |
|---|---|
| id / user_id | 事件与用户标识 |
| source_type / source_id | 来自对话、课程、测试还是发音评测 |
| observed_at | 出现时间 |
| learner_text | 学习者原表达 |
| corrected_text | 推荐表达 |
| error_type | 发音、声调、词汇、语法、汉字、语用、听辨等 |
| subtype | 例如量词、把字句、z/zh、二声/三声 |
| explanation | 符合用户水平的短解释 |
| confidence | 模型判断置信度 |
| severity | 是否阻碍理解、是否命中本课目标 |
| evidence_span | 对应的转写或题目片段 |
| hsk_tags | 对应 HSK 级别、语法点、词条 |
| status | candidate / confirmed / rejected / mastered |
| occurrence_count | 去重合并后的次数 |
| stability / difficulty | FSRS 记忆状态 |
| next_review_at | 下次复习时间 |
| model_version / skill_version | 生成该判断的模型与 Skill 版本 |

### 8.4 记忆写入规则

- 开放式对话中的一次低置信度错误先记为 candidate；
- 同类错误重复出现，或用户确认后，才升级为 confirmed；
- 用户驳回后保留审计事件，但不再作为学习弱点；
- 高风险结论不得仅由模型写入；
- 每晚异步合并重复错误、检查冲突并衰减陈旧偏好；
- 所有长期记忆在“学习档案 → AI 记忆”中可见、可改、可删。

---

## 9. AI 模型与路由设计

### 9.1 推荐模型矩阵

| 任务 | 默认模型 | 模式 | 备注 |
|---|---|---|---|
| 课程侧边问答 | DeepSeek-V4-Flash | 非思考 | 低延迟、受本课 Context Pack 限制 |
| 会话总结 | DeepSeek-V4-Flash | 非思考 + JSON | 异步生成结构化摘要 |
| 错误提取 | DeepSeek-V4-Flash | 非思考 + JSON | 必须经过 Schema 校验和去重规则 |
| 学习计划 | DeepSeek-V4-Flash | 视复杂度切换 | 计划必须引用确定性画像 |
| 课程初稿 | DeepSeek-V4-Pro | 思考 | 后台批处理，可缓存 |
| 课程自动评审 | 与生成模型不同的备选模型 | 独立评审 | 减少同模型自评偏差 |
| 实时语音 | Qwen3.5-Omni-Flash-Realtime | WebRTC | 负责语音、转写、VAD、打断 |
| 发音专项评分 | 科大讯飞 ISE | WebSocket | 仅对明确朗读题调用 |
| 向量化 | text-embedding-v4 | 1024 维 | 课程与用户记忆分 namespace |

DeepSeek 官方在 2026-04-24 上线 V4-Pro 与 V4-Flash；旧 model 名 deepseek-chat 和 deepseek-reasoner 已于 2026-07-24 停止使用，因此代码必须直接使用 deepseek-v4-flash / deepseek-v4-pro。

### 9.2 Provider 抽象

不要做一个“所有模型都长得一样”的过度抽象。至少分成：

- TextGenerationProvider；
- RealtimeVoiceProvider；
- PronunciationAssessmentProvider；
- EmbeddingProvider；
- ModerationProvider。

每类 provider 声明 capabilities，例如 streaming、JSON、tools、thinking、audio_in、audio_out、WebRTC、data_region。路由器依据任务、延迟预算、价格、健康状态和用户选择进行分配。

### 9.3 实时语音成本控制

Qwen3.5-Omni-Realtime 官方说明，多轮对话会重复计费仍在上下文中的历史音频；其音频 Token 换算约为输入每秒 7 Token、输出每秒 12.5 Token。因此必须：

- 使用 3–5 分钟滚动摘要；
- 删除已压缩的旧会话项；
- 实时会话默认限制 10–15 分钟；
- 课件全文不进入音频上下文；
- 为每个用户设置日/月语音预算；
- 上线前实测“每有效对话分钟成本”，不能只按单轮报价估算。

按华北 2 当前公开原价，qwen3.5-omni-flash-realtime 的音频输入约 27 元/百万 Token、文本+音频输出约 107 元/百万 Token。若只计算当前新增音频且双方各说一半时间，8 分钟对话的理论下限约 0.37 元；多轮历史重复计费、文本和基础设施会使实际值更高，本数字只能用于早期预算下限。

---

## 10. 技术架构

### 10.1 建议技术栈

| 层 | 选择 |
|---|---|
| 前端 | Next.js + TypeScript + Tailwind CSS + shadcn/ui |
| 状态与数据请求 | TanStack Query；局部实时状态使用轻量 store |
| 后端 API | FastAPI + Pydantic |
| 实时链路 | 浏览器 WebRTC；后端代理 SDP 鉴权交换并接收必要会话事件，长期 API Key 不进入浏览器 |
| 异步任务 | Redis + ARQ/Celery，MVP 任选其一，不同时引入 |
| 数据库 | PostgreSQL + pgvector |
| ORM/迁移 | SQLAlchemy 2 + Alembic |
| 对象存储 | 阿里云 OSS 或兼容 S3 的 MinIO |
| 课程内容 | MDX + Git 版本库 + 发布编译任务 |
| 观测 | OpenTelemetry + 结构化日志 + Sentry 类错误追踪 |
| 本地开发 | Docker Compose |
| 部署 | 中国内地云主机/容器 + 托管 PostgreSQL/Redis/OSS |

### 10.2 架构图

~~~mermaid
flowchart LR
    U["浏览器 Web App"] --> A["FastAPI / Auth / Workspace API"]
    U <-->|"WebRTC 音频"| Q["Qwen Realtime"]
    A --> R["Agent Orchestrator 与 Model Router"]
    R --> D["DeepSeek 文本模型"]
    R --> E["Embedding 模型"]
    R --> P["发音专项评测"]
    A --> DB[("PostgreSQL + pgvector")]
    A --> O[("OSS / 音频与原始资料")]
    A --> J["异步任务队列"]
    J --> R
    J --> W["课程 Wiki 编译与记忆整理"]
    W --> DB
    W --> G["版本化 MDX / Git"]
~~~

### 10.3 代码仓库建议

~~~text
apps/
├─ web/                 # Next.js
└─ api/                 # FastAPI
packages/
├─ ui/
├─ shared-types/
└─ chinese-learning-coach/
content/
├─ raw/                 # 原始资料及授权元数据
├─ wiki/                # 编译后的教学知识
└─ lessons/             # 已发布课程
infra/
├─ docker/
└─ migrations/
evals/
└─ tutor/
~~~

### 10.4 关键 API

- POST /auth/*：登录与会话；
- POST /onboarding/assessment：建档与诊断；
- GET /workspaces：左侧工作区列表；
- POST /voice/sessions：创建实时会话及客户端安全配置；
- POST /voice/sessions/{id}/offer：后端代理 Qwen WebRTC SDP 鉴权交换；
- POST /voice/sessions/{id}/finalize：结束并触发总结；
- GET /voice/sessions/{id}/report：轮询/订阅总结结果；
- GET /lessons/{id}：课件与当前进度；
- POST /lessons/{id}/ask：课程侧边问答；
- POST /evidence：提交练习证据；
- GET /errors：错误簿；
- PATCH /errors/{id}：确认、驳回、编辑；
- GET /reviews/today：今日复习队列；
- POST /reviews/{id}/grade：提交 FSRS 评分；
- GET /profile/memory：可解释的 AI 记忆；
- DELETE /profile/memory/{id}：删除长期记忆；
- GET /admin/models：模型能力与健康状态。

---

## 11. 核心数据模型

| 表 | 作用 |
|---|---|
| users | 账户与基础信息 |
| consents | 隐私授权、音频保留和政策版本 |
| learner_missions | 目标、成功标准、期限、约束 |
| learner_profiles | 当前聚合画像与各能力估计 |
| profile_facts | 带来源、置信度、有效期的长期事实 |
| workspaces | 对话、课程、复习的统一工作区 |
| sessions | 会话元数据、模型和 Skill 版本 |
| utterances | 用户/AI 转写、时间戳和轮次 |
| session_summaries | 关键内容、亮点、下一步 |
| error_events | 单次错误证据 |
| error_clusters | 去重后的长期错误项 |
| mastery_items | 词汇、语法、发音和技能掌握状态 |
| review_cards | FSRS 状态与到期时间 |
| review_logs | 每次复习评分 |
| courses / lesson_versions | 课程和不可变版本 |
| lesson_progress | 学习进度与完成证据 |
| knowledge_sources | 原始来源、授权和哈希 |
| knowledge_pages | Wiki 页面、引用和发布状态 |
| embeddings | 多 namespace 的向量索引 |
| model_runs | 模型、延迟、Token、成本、状态 |
| audit_logs | 画像更改、删除和管理员操作 |

所有业务表应包含 tenant/user 范围；向量检索必须强制带 user_id 或 public-course namespace，避免跨用户召回。

---

## 12. HSK 内容策略

教育部发布的《国际中文教育中文水平等级标准》采用“三等九级”，以音节、汉字、词汇、语法为四维基准，并覆盖听、说、读、写、译。当前公开 HSK 资源同时存在传统 1–6 级大纲与 7–9 级样题，因此内容系统必须显式版本化，不能把不同标准混成一个模糊的 HSK 3.0 标签。

建议内容标签至少包含：

- framework：GF0025-2021 / current_hsk_1_6 / hsk_7_9；
- level；
- skill：听、说、读、写、译；
- linguistic_item：音节、汉字、词汇、语法；
- task_type；
- source_id、license、source_page；
- reviewed_by、reviewed_at；
- content_version。

首版只使用：

- 教育部公开标准；
- 汉语考试服务网公开大纲与样题；
- 自研例句和练习；
- 明确获得授权的教材内容。

课件应“依据标准重新创作”，而不是大段复制教材或真题。每条考试事实和课程引用需可追溯到来源。

---

## 13. 评测、质量与安全

### 13.1 Agent 评测集

在上线前建立至少 100 个金标准案例：

- 30 个语法错误；
- 20 个词汇与搭配错误；
- 20 个声调、声母、韵母问题；
- 15 个语用和礼貌策略问题；
- 15 个“不应纠正”的正确或可接受表达。

每次更换模型、提示词或 Skill 版本，检查：

- 错误检出准确率；
- 错误误报率；
- 解释是否超出用户水平；
- 建议表达是否自然；
- 是否保存了错误的长期记忆；
- JSON Schema 通过率；
- 引用存在率。

### 13.2 工程测试

- 单元测试：FSRS、错误去重、权限、状态聚合；
- Provider 合同测试：流式、超时、429、JSON 异常、fallback；
- WebRTC 测试：建连、断网重连、耳机切换、打断、回声；
- E2E：建档 → 对话 → 总结 → 确认错误 → 到期复习；
- 数据隔离测试：任何检索不得返回其他用户内容；
- 内容回归：每个发布课程的答案和引用都可验证；
- 压力测试：20 个并发实时会话的可观测性与费用保护。

### 13.3 MVP 验收指标

| 指标 | 目标 |
|---|---|
| WebRTC 建连成功率 | ≥ 98% |
| VAD 结束到首个 AI 音频 | P50 ≤ 1.2 秒，P95 ≤ 2.5 秒 |
| 对话结束后报告生成 | 95% 在 60 秒内完成 |
| 结构化输出 Schema 通过率 | ≥ 99%（含自动修复后） |
| 用户确认后的错误误报率 | < 5% |
| 跨用户数据泄漏 | 0 |
| 课程断点恢复 | 100% |
| 核心 E2E 流程 | 桌面 Chrome、Edge、Safari 可完成 |

产品验证指标建议观察：

- 首次访问完成诊断并开始第一次对话的比例；
- 每周有效说中文分钟数；
- 错误被确认和被驳回的比例；
- 到期复习完成率；
- 4 周后相同错误复现率；
- 4 周模拟题或真实任务完成度变化；
- D1、D7、D30 留存。

---

## 14. 隐私、合规与内容治理

以下为产品工程建议，不替代正式法律意见。

### 14.1 个人信息

- 原始语音可能包含声纹等敏感信息，默认只做实时处理；
- 若保存录音，必须单独、明确同意，说明用途和保留期限；
- 默认长期保存结构化学习证据和转写，不长期保存音频；
- 提供导出、修正、删除账户和删除记忆能力；
- 用户标识传给模型供应商时使用随机内部 ID，不包含姓名、护照号等隐私；
- 记录数据地域和每个 provider 的数据处理范围；
- 不把用户对话默认用于训练自有模型；
- 14 岁以下用户首版不开放；后续若覆盖未成年人，需独立设计监护人同意和未成年人模式。

### 14.2 生成式 AI

- 页面明确标识用户正在与 AI 教练交互；
- 导出的 AI 文本、音频或课件按适用规则添加显式/隐式标识；
- 高风险生活场景如医疗只教学语言表达，不给出诊断；
- 建立违法、有害、歧视、性骚扰和自伤内容的处置策略；
- 公开服务上线前完成适用的生成式 AI、算法、备案和内容标识合规评估。

### 14.3 内容版权

- 原始资料必须记录来源、权利状态和允许用途；
- 未授权教材仅可由用户在合法范围内个人使用，不进入公共课程；
- 官方样题可引用结构和少量示例，商业化前复核具体许可；
- AI 生成内容必须做重复度与事实检查；
- 删除来源时可追踪并重新编译受影响的 Wiki 与课程。

---

## 15. 10 周开发路线图

### 第 1 周：产品冻结与工程骨架

- 确认核心用户、HSK 范围、英文辅助界面；
- 建 monorepo、Docker Compose、CI、环境配置；
- 建立 Chinese Learning Coach Skill v0.1；
- 定义数据模型、错误分类、Lesson Schema 和评测集；
- 完成 3 个可点击低保真页面。

**出口条件**：一条用户旅程、数据库 ERD、Skill 合同和 100 个评测案例框架通过评审。

### 第 2 周：账户、工作区和建档

- 登录与 consent；
- Mission、偏好、初步诊断；
- Codex 风格三栏布局；
- 工作区创建、列表、恢复；
- Model Registry 基础表。

**出口条件**：新用户能完成建档，生成并恢复一个工作区。

### 第 3–4 周：实时对话

- Qwen Realtime WebRTC 接入；
- VAD、打断、转写、状态 UI；
- 场景模板与三种纠错模式；
- 断线、超时、预算保护；
- 结束会话并保存转写。

**出口条件**：连续完成 8 分钟单 Provider 对话，P95 延迟达到目标，无 API Key 暴露；更长训练通过分段会话衔接。

### 第 5 周：总结、错误簿与长期记忆

- DeepSeek 结构化总结；
- error_event 和 error_cluster；
- 用户确认/驳回；
- pgvector 召回；
- 画像聚合、来源解释和记忆删除。

**出口条件**：一次对话能形成可核对错误，并在下一次会话中被正确召回。

### 第 6–7 周：课程系统

- 课程 Wiki 与发布流程；
- 6 节生活课 + 6 节 HSK 课；
- 当前课 Context Pack；
- 右侧课程问答；
- 练习、反馈和学习证据；
- 课程版本与断点恢复。

**出口条件**：用户可完整学习一课、提问、练习并生成掌握证据。

### 第 8 周：复习系统

- FSRS 接入；
- 复习偏好与提醒队列；
- 错误转多种练习；
- Again / Hard / Good / Easy；
- 今日任务和历史曲线。

**出口条件**：确认的错误能按计划到期，完成复习后正确更新下次时间。

### 第 9 周：质量、安全和可观测

- Agent 回归评测；
- 权限与跨用户隔离；
- 费用、延迟、错误监控；
- 数据删除与导出；
- AI 标识和内容安全；
- 浏览器兼容与无障碍。

**出口条件**：核心 E2E、权限、安全和费用熔断全部通过。

### 第 10 周：封闭 Beta

- 招募 20–50 名测试者；
- 每人完成至少 3 次对话、2 节课程和 2 次复习；
- 收集错误误报、延迟、费用和留存；
- 只修复阻断主闭环的问题；
- 输出 MVP 复盘与下一阶段路线。

**出口条件**：核心闭环可重复完成，并拿到是否继续投入的真实数据。

---

## 16. 初始 Backlog

### Epic 1：Learner Identity

- 建档、Mission、偏好、诊断、能力雷达；
- consent 与数据保留；
- AI 记忆查看、修正与删除。

### Epic 2：Live Conversation

- WebRTC 会话、场景、VAD、打断、转写；
- 纠错模式、速度设置、会话总结；
- 费用和时长限制。

### Epic 3：Learning Memory

- error_event、聚类、置信度；
- 证据账本、画像聚合、向量召回；
- 下一次会话 Context Pack。

### Epic 4：Course Workspace

- 内容入库、Wiki 编译、课程发布；
- 课件阅读、侧边问答、练习；
- 进度与学习记录。

### Epic 5：Review

- FSRS、今日队列、用户节奏；
- 多类型复习题；
- 掌握趋势。

### Epic 6：Model Ops

- Provider registry、健康检查、fallback；
- Token、费用、延迟；
- Prompt 与 Skill 版本。

---

## 17. 人力与预算建议

### 17.1 最小团队

- 1 名全栈/Agent 开发者；
- 1 名兼职国际中文教师，每周 4–8 小时；
- 1 名兼职 UI/UX 设计支持，前 3 周集中投入；
- 1 名法律/数据合规顾问，在 Beta 前做一次专项检查。

如果只有开发者一人，必须缩减到 6 节课程、4 个对话场景，并请外部中文教师至少完成一次内容抽检。

### 17.2 Beta 预算框架

不含人工成本，先为以下项目设上限：

- 云主机、数据库、Redis、OSS；
- 文本模型；
- 实时语音模型；
- 发音评测；
- 日志和错误追踪；
- 域名、短信或邮件；
- 教师内容审核。

建议从“每位周活用户的有效对话分钟数”反推预算，并设置：

- 单会话最长 15 分钟；
- 每人每日语音上限；
- 月度 provider 预算；
- 达到 80% / 95% 时告警和熔断；
- 每次模型价格更新后重新计算成本。

---

## 18. 主要风险与缓解

| 风险 | 影响 | 缓解 |
|---|---|---|
| 实时语音延迟或断线 | 核心体验失败 | WebRTC、连接预热、短时会话、断线重连、文本降级 |
| ASR 对外国口音误判 | 错误画像被污染 | 转写可编辑；低置信度不进长期记忆；发音专项用专业评测 |
| 纠错过多导致挫败 | 留存下降 | 每轮最多 1 个重点；默认教练模式；表扬具体进步 |
| 长期记忆写错 | 后续教学持续偏离 | candidate → confirmed；保留证据；用户可驳回和删除 |
| 实时上下文重复计费 | 成本失控 | 滚动摘要、裁剪旧项、时长与预算上限 |
| HSK 标准和大纲混用 | 课程不可信 | framework/version 强制字段；只引用官方来源 |
| 教材版权不清 | 无法商业化 | 来源与授权元数据；发布前版权检查；优先自研内容 |
| 模型供应商变化 | 服务中断 | Provider 分层、能力矩阵、fallback、模型快照 |
| 首版范围过大 | 延期且无闭环 | 严格锁定 HSK 2–4、12 课、6 场景、成人用户 |
| 提示词被轻易复制 | IP 壁垒薄弱 | 把壁垒放在 Taxonomy、Rubric、评测集、内容图谱和效果数据 |

---

## 19. 开发前建议确认的产品假设

本计划默认以下选择，若其中任一项不同，应在第 1 周调整范围：

1. 首批用户为 18 岁以上来华留学生；
2. 首版辅助界面语言为英语；
3. 首版重点为 HSK 2–4，不覆盖 1–9 全部级别；
4. 首版部署在中国内地；
5. 原始语音默认不长期保存；
6. 用户可以选择文本模型，但实时语音 provider 由系统固定；
7. 公共课程只使用官方公开资料、授权资料和自研内容；
8. 首版目标是验证学习闭环，不以收费为首要目标。

---

## 20. 立项后的第一批交付物

计划通过后，建议依次产出：

1. PRD v0.1 与 5 条完整用户故事；
2. 信息架构和 Codex 风格高保真原型；
3. 数据库 ERD 与事件模型；
4. Chinese Learning Coach Skill v0.1；
5. Agent 金标准评测集；
6. 3 个技术 Spike：
   - Qwen WebRTC 8 分钟打断对话；
   - DeepSeek 错误提取 Schema 稳定性；
   - Postgres + pgvector 的跨语言错误召回；
7. 通过 Spike 数据再冻结正式开发排期。

---

## 21. 调研依据

- [DeepSeek API 更新日志：V4-Pro / V4-Flash](https://api-docs.deepseek.com/zh-cn/updates/)
- [DeepSeek 模型与价格](https://api-docs.deepseek.com/quick_start/pricing/)
- [Qwen-Omni-Realtime 官方文档](https://help.aliyun.com/zh/model-studio/realtime)
- [阿里云百炼模型价格](https://help.aliyun.com/zh/model-studio/model-pricing)
- [科大讯飞流式语音评测 API](https://www.xfyun.cn/doc/Ise/IseAPI.html)
- [text-embedding-v4 模型信息](https://help.aliyun.com/zh/model-studio/text-embedding-v4)
- [BGE-M3 官方仓库文档](https://github.com/FlagOpen/FlagEmbedding/blob/master/docs/source/bge/bge_m3.rst)
- [LangGraph 长期记忆分类：语义、情景、程序性记忆](https://langchain-ai.github.io/langgraphjs/how-tos/manage-conversation-history/)
- [Andrej Karpathy：LLM Wiki 原始 Gist](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f)
- [FSRS 开源调度器](https://github.com/open-spaced-repetition/free-spaced-repetition-scheduler)
- [教育部：《国际中文教育中文水平等级标准》](https://www.moe.gov.cn/jyb_xwfb/gzdt_gzdt/s5987/202103/t20210329_523304.html)
- [汉语考试服务网资源中心：大纲与样题](https://admin.chinesetest.cn/godownload.do)
- [中华人民共和国个人信息保护法](https://www.samr.gov.cn/wljys/gzzd/art/2023/art_3ef1e889c1e644d4b65b5f5c7f432386.html)
- [生成式人工智能服务管理暂行办法](https://www.cac.gov.cn/2023-07/13/c_1690898327029107.htm)
- [人工智能生成合成内容标识办法](https://www.cac.gov.cn/2025-03/14/c_1743654685899683.htm)

---

## 22. 最终建议

这个 MVP 最值得验证的不是“AI 会不会讲中文知识”，而是：

> 它能否在低延迟真实对话中发现少量高价值问题，把问题可靠地写入可解释的长期记忆，并在正确时间用合适练习让用户真正改掉。

只要这个闭环成立，课程数量、模型数量和 HSK 覆盖面都可以随后扩展。若一开始同时追求全级别题库、多模型自由切换、全模态、教师后台和移动 App，反而很难知道产品是否真的改善了学习效果。
